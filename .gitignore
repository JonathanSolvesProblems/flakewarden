# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Generated artifacts (regenerate with the scripts; safe to commit if you prefer)
seeded_suite/history.jsonl

# Local env / secrets
.env
*.local

# Devpost submission draft (not part of the deliverable repo)
submission.md

# Demo media (voiceover + b-roll clips; the final video goes to YouTube/Devpost)
broll/
# Stray screen-recording exports at the repo root
2026-*.mp4
# Rendered demo video + vertical Short (large; hosted on YouTube/Devpost, not committed)
FlakeWarden-*.mp4
demo*.mp4

# Presentation deck binaries (hosted externally; large, not committed)
FlakeWarden-*.pdf
FlakeWarden-*.pptx
# Official AgentHack template example (third-party content, not ours to publish)
Submission deck.pptx

# OS / editor
.DS_Store
.vscode/
.idea/
